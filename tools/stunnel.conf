; stunnel 配置：把本机明文端口桥接到 ESP32 的 Modbus TLS 端口
; 用途：让不支持 TLS 的 Modbus 工具（Modbus Poll 等）也能走加密通道

[modbus-tls]
client = yes
accept  = 127.0.0.1:5020
connect = 192.168.10.18:802
CAfile  = /Users/alvin.xie/.stunnel/esp32c5_cert.pem
verifyChain = yes

; 注意：设备 IP 变化时（DHCP 重连）改上面 connect 地址
